// ImageRead.hpp - example I/O node header
#pragma once

// Placeholder for a real node class. Implement node API hooks here.
struct ImageReadNode {
    // Inputs: filename (string)
    // Outputs: cv::Mat image, metadata
};
