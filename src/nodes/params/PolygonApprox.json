{
  "class_name": "PolygonApprox",
  "caption": "PolygonApprox",
  "category": "Contour",

    "inputs": [
        { "name": "points", "type": "PORT_IN" }
    ],

    "outputs": [
        { "name": "out", "type": "PORT_OUT" }
    ],

    "params": {
        "epsilon": { "type": "double", "default": 1.0 },
        "closed": { "type": "bool", "default": true }
    }
}
