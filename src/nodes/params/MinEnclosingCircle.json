{
  "class_name": "MinEnclosingCircle",
  "caption": "MinEnclosingCircle",
  "category": "Contour",

    "inputs": [
        { "name": "points", "type": "PORT_IN" }
    ],

    "outputs": [
        { "name": "center", "type": "PORT_OUT" },
        { "name": "radius", "type": "PORT_OUT" }
    ],

    "params": {
        "epsilon": { "type": "double", "default": 1.0 },
        "closed": { "type": "bool", "default": true }
    }
}
